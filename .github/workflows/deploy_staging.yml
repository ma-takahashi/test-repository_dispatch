name: Deploy staging

on:
  push:
    branches:
      - staging
  repository_dispatch:
    types:
      - update_draft
    branches:
      - staging

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cat README.md
